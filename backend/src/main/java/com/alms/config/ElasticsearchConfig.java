package com.alms.config;

import org.springframework.context.annotation.Configuration;

/**
 * Elasticsearch is auto-configured via spring.elasticsearch.uris in application.yml.
 * Add custom index templates or codec overrides here if needed.
 */
@Configuration
public class ElasticsearchConfig {
}
